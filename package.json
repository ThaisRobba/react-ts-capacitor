{
  "name": "cap-react",
  "version": "1.0.0",
  "main": "src/index.html",
  "license": "MIT",
  "scripts": {
    "android": "npx cap open android",
    "sync": "npx cap sync",
    "dev": "parcel src/index.html -p 3000 --open",
    "build": "rm -rf dist && NODE_ENV=production parcel build src/index.html && npx cap copy"
  },
  "dependencies": {
    "@capacitor/android": "2.4.1",
    "@capacitor/cli": "^2.4.1",
    "@capacitor/core": "^2.4.1",
    "es6-promise-plugin": "^4.2.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.0.2"
  }
}
